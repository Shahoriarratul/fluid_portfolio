import React from 'react';

type Props = {};

function Works({}: Props) {
  return (
    <section
      id='works'
      className='flex flex-col min-h-screen'
    ></section>
  );
}

export default Works;
